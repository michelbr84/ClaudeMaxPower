---
name: using-worktrees
description: DEPRECATED — use /superpowers:using-git-worktrees instead. ClaudeMaxPower no longer ships its own copy of this skill.
deprecated: true
---

# DEPRECATED

This skill was a direct adaptation of `superpowers:using-git-worktrees`.
ClaudeMaxPower no longer ships its own copy.

Install Superpowers for the canonical version:

```
/plugin install superpowers@claude-plugins-official
```

Then use `/superpowers:using-git-worktrees`.
